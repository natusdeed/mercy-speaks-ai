{
  "name": "mercyspeaks-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev -w my-app",
    "build": "npm run build -w my-app",
    "start": "npm run start -w my-app",
    "lint": "npm run lint -w my-app",
    "install-deps": "node scripts/install.js",
    "dashboard:bootstrap": "node scripts/dashboard-bootstrap.mjs"
  },
  "workspaces": [
    "my-app"
  ],
  "dependencies": {
    "@supabase/supabase-js": "^2.47.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.37",
    "typescript": "^5.9.3"
  }
}
