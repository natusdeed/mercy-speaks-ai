# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# Vite
/dist

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo


.env.local
```

4. **Save the file**

**Why?** This makes sure your secret API key doesn't get uploaded to the internet!

**Step 4: Create a Folder for Components**

1. Look in your project for a folder called `src`
2. Inside `src`, look for or create a folder called `components`
3. Your structure should look like:
```
your-project/
├── src/
│   ├── components/     ← We need this folder
│   ├── App.jsx
│   └── main.jsx
├── .env.local               ← Your API key is here
├── .gitignore
└── package.json
